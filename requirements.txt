Django==5.1.4
djangorestframework==3.15.2
drf-spectacular==0.28.0
django-htmx==1.19.0

# 실제 프로토콜 어댑터 (INTERFACE_LIVE_MODE=True 일 때 사용)
# 미설치 상태에서는 lazy-import 가 실패 → Mock 으로 자동 폴백
requests==2.32.3
zeep==4.3.1            # SOAP
paramiko==3.5.0        # SFTP
pika==1.3.2            # AMQP (RabbitMQ) — IBM MQ 는 별도 pymqi 필요
